// Tipos para a página {{titleCase name}}

{{#if withFilters}}
export interface {{pascalCase name}}Props {
  // Definir props se necessário
}
{{/if}}

// TODO: Definir interfaces e tipos específicos da página
export interface {{pascalCase name}}Item {
  id: number;
  // Adicionar propriedades conforme necessário
} 