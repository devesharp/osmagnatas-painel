"use client";

import { Button } from "@/components/ui/button";
import { {{pascalCase name}}Ctrl } from "./{{kebabCase name}}.ctrl";
import { ViewFormProvider } from "@devesharp/react-hooks-v2";
import { Input } from "@/components/ui/input";
import { Select } from "@/components/ui/select";
import { Switch } from "@/components/ui/switch";
import { Textarea } from "@/components/ui/textarea";
import { Checkbox } from "@/components/ui/checkbox";
import { RadioGroup } from "@/components/ui/radio-group";
import { LoadingForeground } from "@/components/loading-foreground";

export function {{pascalCase name}}() {
  const ctrl = {{pascalCase name}}Ctrl();

  return (
    <div className="w-full flex flex-col h-full relative">
      <LoadingForeground visible={ctrl.viewForm.isLoading || ctrl.viewForm.isSaving} />
      <div className="w-full flex-1 overflow-y-auto p-5">
        {/* Header */}
        <div className="pb-8">
          <div className="mx-auto">
            <div className="flex justify-between items-center">
              <div>
                <h1 className="text-2xl font-semibold">
                  {{titleCase name}}
                </h1>
              </div>
            </div>
          </div>
        </div>

        <div className="max-w-6xl">
          <ViewFormProvider {...ctrl.viewForm}>
            <div className="space-y-8">
              {/* Seção Principal */}
              <div className="">
                <h2 className="font-semibold mb-4 border-b pb-2">
                  Configurações Principais
                </h2>

                <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
                  <Input name="name" title="Nome" />
                  <Input name="description" title="Descrição" />
                  <Input name="code" title="Código" />
                </div>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
                  <Input name="email" title="E-mail" type="email" />
                  <Input 
                    name="phone" 
                    title="Telefone" 
                    mask="phone"
                    placeholder="(00) 0000-0000"
                  />
                </div>
              </div>

              {/* Seção Adicional */}
              <div className="">
                <h2 className="font-semibold mb-4 border-b pb-2">
                  Configurações Adicionais
                </h2>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
                  <Switch name="active" title="Ativo" />
                  <Switch name="notifications" title="Notificações" />
                </div>

                <div className="grid grid-cols-1 gap-4">
                  <Textarea 
                    name="notes" 
                    title="Observações" 
                    placeholder="Digite suas observações aqui..."
                  />
                </div>
              </div>

              {/* TODO: Adicionar mais seções conforme necessário */}
            </div>
          </ViewFormProvider>
        </div>
      </div>
      
      {/* Botões de Ação */}
      <div className="gap-3 border-t border-border bg-muted px-3">
        <div className="max-w-6xl justify-end flex h-14 items-center">
          <Button onClick={() => ctrl.viewForm.submitForm()}>
            Salvar
          </Button>
        </div>
      </div>
    </div>
  );
} 