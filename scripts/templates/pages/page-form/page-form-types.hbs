// Tipos para a página {{titleCase name}}

export interface {{pascalCase name}}Props {
  // Definir props se necessário
}

// Interface para os dados do formulário
export interface I{{pascalCase name}}Form {
  id?: number;
  NOME?: string;
  DESCRICAO?: string;
  VIDEO?: string;
  // TODO: Adicionar campos específicos do formulário
} 