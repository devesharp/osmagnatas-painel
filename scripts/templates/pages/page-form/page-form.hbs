"use client";

import { Button } from "@/components/ui/button";
import { {{pascalCase name}}Ctrl } from "./{{kebabCase name}}.ctrl";
import { ViewFormProvider } from "@devesharp/react-hooks-v2";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { LoadingForeground } from "@/components/loading-foreground";
import { UploadImagesInput } from "@/components/upload-images-input";

export function {{pascalCase name}}() {
  const ctrl = {{pascalCase name}}Ctrl();

  return (
    <div className="w-full flex flex-col h-full relative">
      <LoadingForeground
        visible={ctrl.viewForm.isLoading || ctrl.viewForm.isSaving}
      />
      <div className="w-full flex-1 overflow-y-auto p-5">
        {/* Header */}
        <div className="pb-8">
          <div className="mx-auto">
            <div className="flex justify-between items-center">
              <div>
                <h1 className="text-2xl font-semibold">
                  Formulário de {{titleCase name}}
                </h1>
              </div>
            </div>
          </div>
        </div>

        <div className="max-w-6xl">
          <ViewFormProvider {...ctrl.viewForm}>
            <div className="space-y-8">
              {/* Seção Principal */}
              <div className="">
                <h2 className="font-semibold mb-4 border-b pb-2">
                  Dados Principais
                </h2>

                <div className="grid grid-cols-1 gap-4 mb-4">
                  <Input
                    name="NOME"
                    title="Nome *"
                    placeholder="Digite o nome"
                  />
                </div>

                <div className="grid grid-cols-1 gap-4 mb-4">
                  <Textarea
                    name="DESCRICAO"
                    title="Descrição"
                    placeholder="Digite a descrição..."
                    rows={4}
                  />
                </div>

                <div className="grid grid-cols-1 gap-4">
                  <Input
                    name="VIDEO"
                    title="Vídeo (URL)"
                    type="url"
                    placeholder="https://www.youtube.com/watch?v=..."
                  />
                </div>

                <div className="pt-5">
                  <div className="text-sm font-semibold mb-2">
                    Fotos
                  </div>
                  <UploadImagesInput
                    valueExistingImages={ctrl.existingImages}
                    onChangeExistingImages={ctrl.setExistingImages}
                    value={ctrl.images}
                    onChange={ctrl.setImages}
                  />
                </div>
              </div>

              {/* Seção Adicional - TODO: Personalizar conforme necessário */}
              <div className="">
                <h2 className="font-semibold mb-4 border-b pb-2">
                  Informações Adicionais
                </h2>
                
                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  {/* TODO: Adicionar campos específicos do formulário */}
                  <div className="text-sm text-muted-foreground">
                    Adicione aqui campos específicos do seu formulário
                  </div>
                </div>
              </div>
            </div>
          </ViewFormProvider>
        </div>
      </div>

      {/* Botões de Ação */}
      <div className="gap-3 border-t border-border bg-muted px-3">
        <div className="max-w-6xl justify-end flex h-14 items-center">
          <Button onClick={() => ctrl.viewForm.submitForm()}>
            Salvar
          </Button>
        </div>
      </div>
    </div>
  );
} 